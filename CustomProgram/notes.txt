Game
    run
    window
    rendering
        texture manager
            load texures/ spritesheet
            get texture
            animations
        tilemap
        background
    object
        player
            move
            push -> block
            interact
            pickup
        block
            push
            trigger switch
        collectable
            pickup
        trap
            trigger
        switch
            state
        exit/goal
        render
    sound
        music
        sound effect
    gamestate
        win
        loose
        reset       
        time 
        map 
        object manager
        
for object collision
    check if obj at X, Y before player moves
    if so check if that object can move (movable and space to move)
    update obj pos like player
    